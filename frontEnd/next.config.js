/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost','https://limitless-authdemo.netlify.app'],
  },
}

module.exports = nextConfig
